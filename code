scorer_one = 'Ruud Gullit'
scorer_two = 'Marco van Basten'

goal_time_1 = 32
goal_time_2 = 54

scorers = scorer_one + str(goal_time_1), scorer_two + str(goal_time_2)

report = f'{scorer_one} scored in the {goal_time_1}nd minute\n {scorer_two} scored in the {goal_time_2}th minute'

print(report)

player = 'Jan Wouters'

first_name = player.find('Jan')
print(first_name)

last_name_len = player.find('Wouters')

print(len(last_name_len))

name_short = 'J. Wouters'

chant = 'Jan!'

print(chant)

good_chant = f'{chant} * {2}'
print(good_chant)

# Gekeken op het internet leek dit juist voor Part 2, maar ik kom er niet uit met len en slicing. En de !=. 
